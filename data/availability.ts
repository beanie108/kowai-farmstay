/**
 * AVAILABILITY DATA
 * ─────────────────────────────────────────────────────────────
 * To mark dates as booked, add an entry to the relevant array below.
 * Format: { from: "YYYY-MM-DD", to: "YYYY-MM-DD" }
 * The "from" date is the arrival day, "to" is the departure day.
 * Both dates and all days in between will show as unavailable.
 *
 * After saving, commit and push to GitHub — the site updates in ~30 seconds.
 * ─────────────────────────────────────────────────────────────
 */

export const farmstayBookings: { from: string; to: string }[] = [
  // Example — remove and add your own:
  // { from: "2026-05-01", to: "2026-05-04" },
];

export const dogBoardingBookings: { from: string; to: string }[] = [
  // Example — remove and add your own:
  // { from: "2026-04-20", to: "2026-04-25" },
];
